<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup Label="Package">
    <Product>Nalu.Maui.Scaffold</Product>
    <Description>A complete, fully drawn application shell for .NET MAUI replacing Shell on iOS and Android: restylable tab bar, nav bar and drawers, popups and bottom sheets, modal presentation, page transitions with shared elements, iOS edge-swipe and Android predictive back, scroll-driven chrome and system-bar styling — every navigation routes through the Nalu engine (guards, lifecycle, intents).</Description>
    <PackageTags>MAUI;shell;scaffold;navigation;tabbar;navbar;flyout;bottomsheet;popup;transitions;shared-elements;predictive-back</PackageTags>
    <RootNamespace>Nalu</RootNamespace>
    <IsAotCompatible>true</IsAotCompatible>
    <ProduceReferenceAssembly>true</ProduceReferenceAssembly>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <MauiEnableXamlCBindingWithSourceCompilation>true</MauiEnableXamlCBindingWithSourceCompilation>
  </PropertyGroup>

  <PropertyGroup Label="Build">
    <!-- Mobile-only Shell replacement (see scaffold-implementation-strategy.md): net10 for now, net11 soon. -->
    <TargetFrameworks>net10.0;net10.0-android;net10.0-ios26.0</TargetFrameworks>

    <SingleProject>true</SingleProject>
    <OutputType>Library</OutputType>

    <!-- Low floor on purpose (repo policy). v2 native sheets (UISheetPresentationController, iOS 15+)
         would be runtime-guarded rather than raising this. -->
    <SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">12.2</SupportedOSPlatformVersion>
    <SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">21.0</SupportedOSPlatformVersion>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>Nalu.Maui.Test</_Parameter1>
    </AssemblyAttribute>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>DynamicProxyGenAssembly2</_Parameter1>
    </AssemblyAttribute>
    <AssemblyAttribute Include="Microsoft.Maui.Controls.XmlnsDefinition">
      <_Parameter1>https://nalu-development.github.com/nalu/scaffold</_Parameter1>
      <_Parameter2>$(RootNamespace)</_Parameter2>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Maui.Controls" Version="$(MauiVersion10)"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Nalu.Maui.Core\Nalu.Maui.Core.csproj" />
    <ProjectReference Include="..\Nalu.Maui.Layouts\Nalu.Maui.Layouts.csproj" />
    <ProjectReference Include="..\Nalu.Maui.Navigation\Nalu.Maui.Navigation.csproj" />
  </ItemGroup>

  <ItemGroup Label="Source generator (packed as an analyzer, not referenced by the library itself)">
    <ProjectReference Include="..\Nalu.Maui.Scaffold.SourceGenerators\Nalu.Maui.Scaffold.SourceGenerators.csproj" ReferenceOutputAssembly="false" PrivateAssets="all" />
    <None Include="..\Nalu.Maui.Scaffold.SourceGenerators\bin\$(Configuration)\netstandard2.0\Nalu.Maui.Scaffold.SourceGenerators.dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
  </ItemGroup>
</Project>
